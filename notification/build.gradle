dependencies {
    // Firebase Admin SDK
    implementation 'com.google.firebase:firebase-admin:8.0.0'
    implementation project(':common')
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}
